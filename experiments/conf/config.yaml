hydra:
  run:
    dir: ./logs/${now:%Y-%m-%d}/${now:%H-%M-%S}
  sweep:
    dir: ./logs/${now:%Y-%m-%d}/${now:%H-%M-%S}
    subdir: ${hydra.job.num}

video_dir: videos
seed: 0
num_episodes: 1000
max_episode_length: 100
record_videos: False
defaults:
  - _self_
  - approach: random
  - env: tiny
